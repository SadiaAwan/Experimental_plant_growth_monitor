[project]
name = "experimental-plant-growth-monitor"
version = "0.1.0"
description = "MQTT pipeline for a Wokwi plant growth monitor"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "paho-mqtt>=2.1.0",
    "psycopg[binary]>=3.3.5",
]